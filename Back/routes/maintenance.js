import { Router } from 'express';

const maintRouter = Router();

/*
 Will need to think properly about the schema and what we expect to be passing
 back and forth here

 Previous milage, task, etc

 How it'll be consumed in front and how we prep it for the back, the query, etc
*/

// maintRouter.get('/')

// maintRouter.post('/')

export default maintRouter;
